const Pipelines = () => {
  return (
    <div>
      <h1 className="text-3xl font-bold">Pipelines</h1>
      <p className="mt-4 text-gray-600">A visão Kanban e de lista para seus pipelines de venda será construída aqui.</p>
    </div>
  );
};

export default Pipelines;